apiVersion: 1

providers:
  - name: DriftGuard
    orgId: 1
    folder: DriftGuard
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards